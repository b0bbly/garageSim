public enum UseType
{
    None,
    MouseClick,    // Use with left mouse button (e.g., wrench, gun)
    Interact,      // Use with E key (e.g., doors, buttons)
    Consume        // Use instantly (e.g., food, ammo)
}